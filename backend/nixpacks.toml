[phases.setup]
nixPkgs = ['nodejs-18_x', 'npm-9_x']

[phases.install]
cmds = ['npm ci']

# 移除构建步骤，直接运行JavaScript

[start]
cmd = 'npm run start:js'

[variables]
NODE_ENV = 'production'
PORT = '8000'
RAILWAY_ENVIRONMENT = 'true'